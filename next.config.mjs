/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            {
                hostname: "2zdozox3olxyjiau.public.blob.vercel-storage.com"
            }
        ]
    }
};

export default nextConfig;
